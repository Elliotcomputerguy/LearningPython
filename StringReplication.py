#!/usr/bin/python
# You can also use the * operator to do string replication.
# The * operator can work with a string and integer data type value. It cannot work with two string values. 'Hello' * 'World'  
var = 'Hello' * 3
print(var)

var = var * 2
print(var)

